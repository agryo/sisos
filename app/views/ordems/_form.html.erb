<%= form_for(@ordem) do |f| %>
  <% if @ordem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ordem.errors.count, "error") %> prohibited this ordem from being saved:</h2>

      <ul>
      <% @ordem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>	
  <% end %>

  <div class="field">
    <%= f.label   :cliente %>
    <%= f.collection_select :cliente_id, Cliente.all, :id,:nome  %>
  </div>
  <div class="field">
    
    <%= f.text_field :maquina   , :placeholder=>"Máquina" , :class=>"input-block-level" %>
  </div>
  <div class="field">
  
    <%= f.text_field :pecas   , :placeholder=>"Peças" , :class=>"input-block-level" %>
  </div>
  <div class="field">

    <%= f.text_area :observacoes   , :placeholder=>"Observações" , :class=>"input-block-level" %>
  </div>
  <div class="actions">
    <button class="btn btn-large btn-primary" type="submit">Adicionar</button>
   
  </div>
<% end %>
